# Stamp Maker - Генератор STL штампов из изображений

## Описание проекта

Веб-приложение на React для генерации 3D моделей штампов (STL файлов) из растровых изображений.

## Основной сценарий использования

1. Пользователь загружает картинку (drag & drop или кнопка выбора)
2. Картинка обрабатывается с применением настроек
3. Пользователь видит превью 3D модели в реальном времени
4. Пользователь скачивает STL файл

## Технологический стек (утверждено)

- **React** + **TypeScript**
- **Vite** - bundler
- **React Three Fiber** (@react-three/fiber) - для 3D
- **@react-three/drei** - для OrbitControls и утилит
- **Material-UI (MUI)** - UI компоненты (богатая библиотека для будущих расширений)
- **WebGL шейдеры** - для обработки изображений (Gaussian blur, uniform blur)
- **STL export** - ASCII формат (STLExporter из three.js или самописный)

## Архитектура интерфейса

### Левая панель - Загрузка, настройки и превью (3/12 ширины)
- Кнопка загрузки изображения
- **Настройки обработки картинки (компактные):**
  - Инвертировать (checkbox)
  - Гаусс (slider, 0-50)
  - Uniform (slider, 0-50)
  - Bezier в одну строку (4 поля x1, y1, x2, y2)
- **Настройки сетки (компактные):**
  - Разрешение (slider, 10-1024)
  - Экструзия (мм, округлено до сотых)
  - Основание (мм, округлено до сотых)
  - Ширина/Высота (мм, округлено до сотых, с auto aspect ratio)
  - Кнопка "Сохранить STL"
- **Превью обработанного изображения внизу панели**

### Центральная область - 3D Preview (9/12 ширины, расширена)
- React Three Fiber Canvas на весь экран
- Ось Z вверх
- Фон #badbee
- Камера: динамическая позиция, target в центре модели
- Штамп плоской стороной вниз (Z=0), рельеф сверху
- OrbitControls без запаздывания (enableDamping=false)
- **Preview обработанного изображения поверх 3D viewer (position absolute, правый верхний угол)**
  - 300px ширина, полупрозрачный белый фон
  - Скроллится если не влезает

## Логика обработки изображения

Pipeline (выполняется на GPU через шейдеры где возможно):

1. **Grayscale конверсия** (всегда) - стандартная формула luminance
2. **Инверсия** (опционально) - 255 - value
3. **Размытие по Гауссу** (WebGL shader, настраиваемая степень)
4. **Uniform размытие с круглым ядром** (WebGL shader, box blur с круглой маской)
5. **Cubic Bezier преобразование** (transfer function для кривой высот)
   - Применяется к значениям яркости для получения высоты
6. **Jump Flooding Distance Field** (в бэклоге, будет WebGL shader):
   - Бинаризация по порогу 127
   - Построение карты расстояний до контура
   - Внешние расстояния: положительные
   - Внутренние расстояния: отрицательные
   - Расстояния в % от min(w,h)
   - Маппинг:
     - 0 → 127 gray
     - >= +ширина_порога → 255 (белый)
     - <= -ширина_порога → 0 (черный)
     - Между: линейная интерполяция

## MVP (Minimum Viable Product) - Первая версия

### Что входит в MVP:
- ✅ Загрузка изображения (кнопка выбора файла)
- ✅ **Обработка через WebGL шейдеры: grayscale + инверсия + gaussian blur + uniform blur + cubic bezier**
- ✅ **Preview обработанного изображения поверх 3D viewer (position absolute)**
- ✅ **Компактные настройки в левой панели**
- ✅ Настройки сетки: resolution + extrusionHeight + baseHeight + width/height с автоподстройкой
- ✅ **Vertex colors - раскраска 3D сетки RGB цветами из исходного изображения**
- ✅ **Автоматический аутлайн по форме объекта (Marching Squares)**
  - Checkbox "Аутлайн по форме"
  - Настройка порога бинаризации (0-255)
  - Настройка отступа от контура (0-20px)
- ✅ 3D viewer с OrbitControls (без запаздывания)
- ✅ **Динамическое центрирование камеры на модель**
- ✅ **Правильные нормали боковых стенок (видны снаружи)**
- ✅ Экспорт в ASCII STL
- ✅ Базовый error handling (try-catch)

### Что НЕ реализовано (откладываем на потом):
- ⏳ Drag & drop для загрузки
- ⏳ localStorage persistence настроек
- ⏳ Jump Flooding Distance Field
- ⏳ Визуальный редактор Cubic Bezier (пока 4 поля)
- ⏳ Loading indicators
- ⏳ Детальная валидация файлов
- ⏳ Debounce для оптимизации
- ⏳ Сглаживание углов аутлайна (пока контур без сглаживания)

## Настройки генерации сетки (MVP)

- Пропорции сетки совпадают с пропорциями картинки
- Разрешение задается по меньшему размеру (макс 1024×1024)
- Высота вершин = grayscale_value / 255 * высота_экструзии
- Основание: высота_основания (плоская часть снизу)
- Материал: простой серый MeshStandardMaterial
- Формат STL: ASCII

## Генерация 3D сетки (MVP)

- PlaneGeometry с разрешением из настроек
- Модификация Z координат вершин согласно heightmap
- Добавление граней для основания и боковых стенок
- Простой серый материал (без vertex colors в MVP)
- Экспорт в ASCII STL формат

## Хранение данных (MVP)

- Картинка и модель хранятся только в оперативной памяти
- localStorage - в будущем
- Форматы входных изображений: все поддерживаемые браузером (PNG, JPEG, WebP, etc)
- Максимальный размер изображения: без ограничений (∞)
- Максимальное разрешение сетки: 1024×1024

## Обработка в реальном времени (MVP)

- Модель пересчитывается при каждом изменении параметра (real-time)
- Если будет тормозить - добавим debounce (в будущем)

## План разработки MVP

### Этап 1: Setup проекта ✅ ЗАВЕРШЁН
- [x] Инициализация Vite + React + TypeScript
- [x] Установка зависимостей:
  - @react-three/fiber
  - @react-three/drei
  - three
  - @mui/material @mui/icons-material @emotion/react @emotion/styled
- [x] Создать структуру папок (components, hooks, utils, shaders, types, store)
- [x] Базовый App.tsx с трёхпанельным layout (Grid/Box)
- [x] Заглушки компонентов: LeftPanel, RightPanel, Viewer3D
- [x] Базовый 3D viewer с OrbitControls и фоном #badbee
- [x] Настроен .npmrc для pnpm (shamefully-hoist)
- [x] Даунгрейд React 19 → 18 для совместимости с R3F
- [x] Отключен strict mode в tsconfig для упрощения (временно)
- [x] Проект успешно собирается (pnpm run build)
- [x] Dev server запускается на http://localhost:5174/

### Этап 2: State Management ✅ ЗАВЕРШЁН
- [x] Определены TypeScript типы (ImageSettings, MeshSettings)
- [x] Создан React Context (AppContext) для:
  - Загруженное изображение (File/URL)
  - Настройки обработки изображения
  - Настройки сетки
  - Обработанное изображение (ImageData)
- [x] Установлены default values (DEFAULT_IMAGE_SETTINGS, DEFAULT_MESH_SETTINGS)
- [x] Создан useAppContext hook
- [x] ~~localStorage persistence~~ - в будущем

### Этап 3: UI Layout (MUI)
- [ ] Создать трёхпанельную структуру с MUI Grid/Box
- [ ] Левая панель: Paper с секциями
- [ ] Центр: Canvas область для R3F
- [ ] Правая панель: Paper с preview изображения
- [ ] ~~Responsive layout~~ - в будущем

### Этап 4: Загрузка изображений (MVP: только кнопка) ✅ ЗАВЕРШЁН
- [x] Компонент ImageUpload
- [x] File input кнопка (MUI Button + input type="file")
- [x] Базовый try-catch для ошибок
- [x] Загрузка в память (FileReader → Data URL)
- [x] Сохранить в Context
- [x] Вычисление aspect ratio изображения
- [x] ~~Drag & drop~~ - в будущем
- [x] ~~Детальная валидация~~ - в будущем

### Этап 5: Настройки в левой панели (MUI компоненты) ✅ ЗАВЕРШЁН
- [x] Секция "Обработка изображения":
  - Checkbox "Инвертировать"
  - Slider "Размытие по Гауссу" (0-20)
  - Slider "Uniform размытие" (0-20)
  - 4 TextField для Cubic Bezier: x1, y1, x2, y2 (defaults: 0,0,1,1)
- [x] Секция "Настройки сетки":
  - Slider "Разрешение" (10-1024)
  - TextField "Высота экструзии" (мм, число)
  - TextField "Высота основания" (мм, число)
  - TextField "Ширина" (мм) → автоподстройка высоты с учётом aspect ratio
  - TextField "Высота" (мм) → автоподстройка ширины с учётом aspect ratio
- [x] Button "Сохранить STL"
- [x] Все изменения → Context (real-time)

### Этап 6: Обработка изображений (Pipeline на WebGL шейдерах) ✅ ЗАВЕРШЁН
- [x] Создана ImageProcessor утилита
- [x] Grayscale конверсия (Canvas API с luminance formula)
- [x] Инверсия (255 - value, Canvas API)
- [x] **Gaussian blur (WebGL shader, two-pass: horizontal + vertical, 9-tap kernel)**
- [x] **Uniform blur с круглой маской (WebGL shader, box blur в круге)**
- [x] **Cubic Bezier transfer function (WebGL shader)**
- [x] **Pipeline: Source → Grayscale → Invert → GaussianBlur → UniformBlur → CubicBezier**
- [x] Результат → ImageData для 3D + Canvas для preview
- [x] Создан хук useImageProcessing для автообработки при изменении настроек
- [x] **shaderBlur.ts - все размытия и bezier на GPU (макс радиус 50)**
- [x] **Исходное изображение сохраняется как ImageData для vertex colors**
- [x] ~~Debounce оптимизация~~ - пока не нужна, шейдеры быстрые

### Этап 7: Preview обработанного изображения ✅ ЗАВЕРШЁН
- [x] Компонент ImagePreview
- [x] **Position absolute поверх 3D viewer (правый верхний угол)**
- [x] **300px ширина, полупрозрачный белый фон (rgba(255,255,255,0.95))**
- [x] Отображение результата обработки на Canvas
- [x] Масштабируемый preview (imageRendering: pixelated)
- [x] Обновление в real-time из Context через useEffect

### Этап 8: Генерация 3D сетки ✅ ЗАВЕРШЁН
- [x] Создана MeshGenerator утилита (generateStampGeometry)
- [x] Функция генерации heightmap geometry:
  - Кастомная генерация вершин с учётом resolution
  - Модификация Z координат вершин по grayscale данным (baseHeight + grayscale * extrusionHeight)
  - Добавление основания (bottom plane на Z=0)
  - **Добавление боковых стенок (4 стороны) - исправлены нормали наружу**
  - Правильный aspect ratio из изображения
  - **Vertex colors из исходного RGB изображения**
- [x] Пересчёт нормалей (computeVertexNormals)
- [x] BufferGeometry с позициями, индексами и **vertex colors**
- [x] **Создана OutlineGenerator утилита (generateOutlineGeometry)**
- [x] **Автоматический аутлайн по форме объекта (Marching Squares)**
  - Бинаризация изображения по порогу
  - Построение контура методом Marching Squares
  - Применение отступа от контура (опционально)
  - Упрощение контура под заданное разрешение
  - Генерация 3D геометрии по контуру (не прямоугольник)

### Этап 9: 3D Viewer (React Three Fiber) ✅ ЗАВЕРШЁН
- [x] Компонент Viewer3D с R3F Canvas
- [x] Настройка сцены:
  - Фон #badbee
  - Освещение (ambientLight 0.6 + 2x directionalLight)
  - Камера up: [0, 0, 1] для Z вверх
- [x] Компонент StampMesh:
  - Генерирует geometry из processedImageData
  - **MeshStandardMaterial с vertexColors** - раскраска из исходного изображения
  - **Поддержка двух режимов: прямоугольный и outline по форме**
  - Автоматическое обновление при изменении imageData или meshSettings
- [x] OrbitControls из @react-three/drei (enableDamping=false, без запаздывания)
- [x] **Камера: динамическая позиция и target (центрируется на модели автоматически)**
- [x] **Target: центр модели по Z (baseHeight + extrusionHeight/2)**
- [x] Обновление модели в real-time при изменении настроек

### Этап 10: STL Export ✅ ЗАВЕРШЁН
- [x] Функция экспорта в ASCII STL (самописная):
  - Итерация по треугольникам через indices
  - Вычисление нормалей для каждого facet
  - Форматирование в ASCII STL формат
  - Экспорт чисел в научной нотации (toExponential)
- [x] Функция скачивания (Blob + createElement('a') + download)
- [x] Привязано к кнопке "Сохранить STL"
- [x] Имя файла: stamp_[timestamp].stl
- [x] Создан хук useSTLExport для удобного использования

### Этап 11: Тестирование и финальные штрихи MVP
- [ ] Тестирование на разных изображениях
- [ ] Проверка экспорта STL и открытия в сторонних программах
- [ ] Базовый README с инструкциями запуска
- [ ] Code cleanup

## После MVP - Улучшения

### Фаза 2: UX улучшения
- [ ] Drag & drop для загрузки изображений
- [ ] Loading indicators
- [ ] Debounce для оптимизации real-time обработки
- [ ] Детальная валидация и error messages
- [ ] Tooltips и улучшенные labels

### Фаза 3: Визуальные улучшения
- [ ] Vertex colors из изображения
- [ ] Улучшенное освещение и материалы
- [ ] Grid helper опционально
- [ ] Антиалиасинг и постобработка

### Фаза 4: Persistence
- [ ] localStorage для настроек
- [ ] Автосохранение при изменении
- [ ] Preset'ы настроек

## Архитектурные решения

### State Management
Используем React Context для простоты (можно мигрировать на Zustand если станет сложнее):
```typescript
interface AppState {
  sourceImage: string | null; // URL or data URL
  processedImageData: ImageData | null;
  imageSettings: ImageSettings;
  meshSettings: MeshSettings;
}

interface ImageSettings {
  invert: boolean;
  gaussianBlur: number;
  uniformBlur: number;
  bezierCurve: [number, number, number, number]; // x1, y1, x2, y2
  jumpFlooding: boolean; // disabled for now
  jumpFloodingThreshold: number; // disabled for now
}

interface MeshSettings {
  resolution: number;
  extrusionHeight: number;
  baseHeight: number;
  width: number;
  height: number;
}
```

### Обработка изображений
- Использовать OffscreenCanvas где доступно для производительности
- WebGL pipeline: Source → Grayscale → Invert → GaussianBlur → UniformBlur → BezierTransfer → Output
- Каждый шаг - отдельный shader pass через RenderTarget
- Cubic Bezier: можно на CPU (для каждого пикселя) или lookup table в шейдере

### 3D Pipeline
- Обработанное изображение → ImageData
- ImageData → Heightmap (массив высот)
- Heightmap + настройки → BufferGeometry
- BufferGeometry → Mesh с vertex colors
- Mesh → R3F Scene

## Бэклог (далёкое будущее)

- [ ] **Улучшение аутлайна**
  - Сглаживание углов контура (Catmull-Rom spline или Bezier)
  - Адаптивное упрощение контура (Douglas-Peucker)
  - Множественные контуры (внутренние и внешние)
- [ ] Jump Flooding Distance Field (WebGL shader)
- [ ] Визуальный редактор Cubic Bezier кривой (2D canvas с draggable handles)
- [ ] Дополнительные фильтры обработки
- [ ] Binary STL export
- [ ] История изменений (undo/redo)
- [ ] Batch processing
- [ ] Экспорт в OBJ, GLTF
- [ ] Dark mode
- [ ] Локализация

## Технические заметки

### WebGL Shaders
Gaussian Blur - двухпроходный (horizontal + vertical) для производительности:
```glsl
// Fragment shader для horizontal pass
uniform sampler2D tDiffuse;
uniform float h; // kernel size
varying vec2 vUv;

void main() {
  vec4 sum = vec4(0.0);
  // Gaussian kernel weights
  // ... blur logic
  gl_FragColor = sum;
}
```

### STL ASCII Format
```
solid stamp
  facet normal 0 0 1
    outer loop
      vertex 0 0 0
      vertex 1 0 0
      vertex 1 1 0
    endloop
  endfacet
  ...
endsolid stamp
```

### Координатная система
Three.js по умолчанию: Y вверх, Z вперед
Наша система: Z вверх
Решение: повернуть модель или камеру, или использовать `up` вектор камеры

## Вопросы для будущего

- [ ] Нужна ли поддержка touchscreen устройств?
- [ ] Нужен ли dark mode?
- [ ] Локализация (i18n)?
- [ ] Аналитика использования?

## Заметки

- Настроек картинки в будущем станет больше - архитектура должна быть расширяемой
- Возможно изменение расположения панелей - layout должен быть гибким
- Центр взаимодействия - этот файл (.claude.md)
- Проект начат: 2025-01-25
- Пользователь использует macOS, shell: sh
- Разработка ведётся итеративно, приоритет - working prototype, потом оптимизация

## Текущий этап

**🎉 MVP ГОТОВ + РАСШИРЕН! Все основные этапы завершены (1-10) + vertex colors + outline**

### ✅ Реализовано в MVP:
- ✅ Загрузка изображений (кнопка выбора файла)
- ✅ **Обработка через WebGL шейдеры: Gaussian blur + Uniform blur + Cubic Bezier (все на GPU)**
- ✅ **Макс радиус размытия: 50px**
- ✅ **Правильный порядок: Grayscale → Invert → GaussianBlur → UniformBlur → CubicBezier**
- ✅ **Preview поверх 3D viewer (position absolute, правый верхний угол, 300px)**
- ✅ **Компактные настройки: Bezier в 1 строку, короткие лейблы, малые отступы**
- ✅ Полные настройки обработки (инверсия, оба размытия, bezier)
- ✅ Полные настройки сетки (resolution, extrusion, base, width/height с auto aspect ratio)
- ✅ **Vertex colors - раскраска 3D модели RGB цветами из исходного изображения**
- ✅ **Автоматический аутлайн по форме объекта (Marching Squares)**
  - Checkbox "Аутлайн по форме"
  - Настройка порога бинаризации (0-255)
  - Настройка отступа от контура (0-20px)
  - Генерация не прямоугольного штампа, а по контуру объекта
- ✅ **Округление размеров до сотых в UI (toFixed(2))**
- ✅ **Дефолты: invert=true, width=50, extrusionHeight=4, baseHeight=5**
- ✅ **Правильные нормали боковых стенок (видны снаружи)**
- ✅ **3D viewer на 9/12 экрана, динамическое центрирование камеры**
- ✅ **OrbitControls без запаздывания (enableDamping=false)**
- ✅ Экспорт в ASCII STL формат
- ✅ Real-time обновление при изменении параметров
- ✅ Проект собирается и работает

### ❌ НЕ реализовано (в бэклоге):

#### Приоритет 1 (улучшения UX):
- ⏳ **Drag & drop для загрузки изображений**
- ⏳ **Vertex colors** - раскраска 3D сетки RGB цветами из обработанного изображения
- ⏳ **localStorage** - сохранение и загрузка настроек
- ⏳ **Debounce** - оптимизация при работе с большими изображениями/высоким разрешением
- ⏳ **Loading indicators** - индикация процесса обработки
- ⏳ **Error handling** - детальные сообщения об ошибках, валидация

#### Приоритет 2 (визуальные улучшения):
- ⏳ **Визуальный редактор Cubic Bezier** - 2D canvas с draggable handles
- ⏳ **Grid helper** - опциональная сетка в 3D для ориентации
- ⏳ **Улучшенное освещение** - настройка теней, ambient occlusion
- ⏳ **Антиалиасинг и постобработка** - улучшение качества рендера
- ⏳ **Responsive layout** - адаптация под мобильные устройства

#### Приоритет 3 (новые фичи):
- ⏳ **Jump Flooding Distance Field** (WebGL shader) - эффекты на основе расстояния до контура
- ⏳ **Автоматический аутлайн по форме объекта** - не прямоугольный штамп, а по контуру
  - Построение аутлайна с отступом (Marching Squares / растеризация)
  - Настройка отступа от краёв
  - Сглаживание углов
- ⏳ **Binary STL export** - для больших моделей (компактнее чем ASCII)
- ⏳ **Preset'ы настроек** - сохранённые комбинации параметров
- ⏳ **История изменений (Undo/Redo)**
- ⏳ **Batch processing** - обработка нескольких изображений
- ⏳ **Экспорт в другие форматы** - OBJ, GLTF
- ⏳ **Дополнительные фильтры** - контраст, яркость, пороговая бинаризация, etc

### Технические решения:
- React 18.3.1 (даунгрейд с 19 для совместимости с R3F)
- pnpm с shamefully-hoist для правильного разрешения зависимостей
- Отключен strict mode в TypeScript (временно, для упрощения разработки MVP)
- @ts-nocheck в Viewer3D (TODO: исправить типы R3F для React)

### Компоненты:
- `src/App.tsx` - **главный компонент с layout, Preview поверх 3D viewer**
- `src/components/LeftPanel/LeftPanel.tsx` - левая панель (загрузка + настройки)
- `src/components/LeftPanel/ImageUpload.tsx` - загрузка файла
- `src/components/LeftPanel/ImageSettings.tsx` - **компактные настройки обработки**
- `src/components/LeftPanel/MeshSettings.tsx` - **компактные настройки сетки (округление до сотых)**
- `src/components/RightPanel/ImagePreview.tsx` - **превью поверх 3D (position absolute)**
- `src/components/Viewer3D/Viewer3D.tsx` - **3D canvas (9/12 экрана, динамическая камера)**
- `src/components/Viewer3D/StampMesh.tsx` - **3D модель штампа (поддержка outline режима)**
- `src/utils/imageProcessor.ts` - **pipeline обработки (все шейдеры)**
- `src/utils/shaderBlur.ts` - **WebGL шейдеры: Gaussian, Uniform, CubicBezier**
- `src/utils/meshGenerator.ts` - **генерация прямоугольной геометрии (vertex colors, исправлены нормали)**
- `src/utils/outlineGenerator.ts` - **генерация геометрии по контуру (Marching Squares)**
- `src/utils/stlExporter.ts` - экспорт в ASCII STL
- `src/hooks/useImageProcessing.ts` - автообработка изображений
- `src/hooks/useSTLExport.ts` - экспорт STL
- `src/store/AppContext.tsx` - глобальный state (с imageAspectRatio)
- `src/store/useAppContext.ts` - hook для доступа к state

### Этап 11: Тестирование и правки MVP ✅ ЗАВЕРШЁН
- [x] **Исправлены нормали боковых стенок (видны снаружи)**
- [x] **Все размытия и bezier переписаны на WebGL шейдеры**
- [x] **Проверен порядок обработки (bezier после блуров)**
- [x] **Установлены правильные дефолты (invert=true, width=50, extrusion=4, base=5)**
- [x] **Preview перемещён в position absolute над 3D viewer**
- [x] **Настройки сделаны компактными (короткие лейблы, малые отступы)**
- [x] **Bezier собран в одну строку (4 поля в grid)**
- [x] **Viewport отцентрован на модель (динамический target)**
- [x] **Размеры округлены до сотых в UI (toFixed(2))**
- [x] **Макс размытие увеличен до 50px**
- [x] **Vertex colors реализованы - раскраска из исходного RGB изображения**
- [x] **Автоматический аутлайн по форме объекта реализован (Marching Squares)**
- [x] **Настройки аутлайна: checkbox, порог бинаризации, отступ от контура**
- [ ] Протестировать на разных изображениях (пользователь тестирует)
</text>

- [ ] Проверить экспорт STL и открытие в слайсере
- [ ] Багфиксы если необходимо

### Следующие улучшения (после тестирования):
- Drag & drop для загрузки
- WebGL шейдеры для производительности (Gaussian blur, uniform blur)
- Vertex colors из изображения
- localStorage persistence
- Debounce для тяжёлых операций

Приоритет: MVP с минимальным функционалом, но полностью рабочий инструмент для генерации STL из изображений.